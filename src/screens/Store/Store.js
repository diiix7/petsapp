import {View, Text} from "react-native";
import StoreStyle from "./Store.style.js";
const Store = (props) => {
	return (
		<View style={StoreStyle}>
			<Text>Store</Text>
		</View>
	);
};
export default Store;